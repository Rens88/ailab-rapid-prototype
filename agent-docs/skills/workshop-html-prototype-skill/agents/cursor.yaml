interface:
  display_name: "Workshop HTML Prototype"
  short_description: "Guide Cursor toward simple standalone HTML workshop prototypes"
  default_prompt: "Update generate_standalone_html.py or safe source inputs to generate a simple mobile-friendly Phase 1 prototype into build/ with a recognizable sequential filename."

workspace:
  preferred_output: "generate_standalone_html.py"
  keep_simple: true
